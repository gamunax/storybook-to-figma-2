export enum TooltipPosition {
  left = 'left',
  right = 'right',
  top ='top',
  bottom = 'bottom',  
  before ='before',
  after = 'after',  
} 

export enum TooltipTouchGestures {
  auto = 'auto',
  on = 'on',
  off = 'off'
}

export enum TooltipVisibility {
  initial = 'initial',
  visible = 'visible',
  hidden = 'hidden'
}